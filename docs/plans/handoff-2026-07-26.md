# Session Handoff — 2026-07-26

Written at the end of the macOS session so work can resume on the Windows laptop.
All code below is committed and pushed to `origin/main` (HEAD = `88462c5`).

## RESUME HERE

Read this block first, then the sections below as needed.

State of play: everything from the macOS session is **done, committed, and pushed**. Nothing is half-written. The remaining work is deploy + on-device verification, in this order:

1. **Collectz deploy (user action, needs their Supabase credentials):**
   - Apply migration `supabase/migrations/20260726120000_collectz_join_approval.sql` (`supabase db push`)
   - `supabase functions deploy collectz-join collectz-remind`
   - Then smoke-test: participant joins an approval-required session → organizer gets push → approve/reject → participant gets push.
2. **Loader verification on device:** cold-open Personal Dashboard, Business Dashboard, Stall, Seller — skeleton/spinner should flash and exit on real data, never stick. TransactionsList was deliberately not touched (user approved its behavior).
3. **Uncommitted files in the tree belong to a parallel agent, not this session** (`docs/plans/premium-grants-and-rewards.md`, `remotion-promo/DEBT_PROMO_RULES.md`, `docs/plans/fiuu-qr-integration.md`). Leave them alone unless the user says otherwise.

Windows constraints: no Xcode — iOS builds/simulator are out; JS/TS, Android, Supabase, and remotion work are fine.

## Done this session (verified, `npx tsc --noEmit` clean)

- **Loader overhaul** — loaders are now data-driven (zustand `persist` hydration signal) with hard caps, replacing fake interaction timers and unbounded spinners:
  - `src/screens/personal/Dashboard.tsx` — hydration-gated, 1.5s cap (was `InteractionManager.runAfterInteractions`)
  - `src/screens/business/Dashboard.tsx` — same pattern
  - `src/screens/stall/Dashboard.tsx` — **added** an initial skeleton loader (it had none; content popped in)
  - `src/screens/seller/Dashboard.tsx` — `isSyncing` spinner now capped at 8s via `syncTimedOut`, resets when sync finishes
- **Collectz join-approval** — organizer can require approval on create/edit session; participant join → organizer push → approve/reject → participant push. Migration + `collectz-join` / `collectz-remind` functions written; deploy pending (above).
- **PurchaseResultModal** (`src/components/common/PurchaseResultModal.tsx`) — floating success/failed modal for subscriptions; inner content animates, modal itself appears without entry animation (explicit user request).
- **Quick Log 3-way hub** — Quick Log opens as hero card + 3 list entries (Apple Log / Backtap Log / Share Log), each drilling into its own screen; settings wording updated to three ways.
- **Remotion debt-screen promo** — white background, ElevenLabs voice "m daud", snap-receipt-and-split narrative; assets from `assets/website jejakbaki/`.

## Decisions & gotchas

- Loader pattern to reuse: `store.persist.hasHydrated()` / `onFinishHydration(cb)` + `setTimeout` cap. Do not reintroduce `runAfterInteractions` gates.
- A parallel agent edits this repo occasionally — **re-read any file immediately before editing it**; stale `old_string` Edits have failed repeatedly.
- User prefers: no entry animation on modals (animate content inside instead), white-background promos, Malay/casual copy voice, `jejakbaki.my` branding.
